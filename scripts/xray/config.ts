export const config = {

    jiraEmail:
      process.env.JIRA_EMAIL_TEST_NOVO || '',
  
    jiraApiToken:
      process.env.JIRA_API_TOKEN_TEST_NOVO || '',
  
    jiraBaseUrl:
      process.env.JIRA_BASE_URL_TEST_NOVO || '',
  
    projectKey:
      process.env.PROJECT_KEY_TEST_NOVO || '',
  
    xrayClientId:
      process.env.XRAY_CLIENT_ID_TEST_NOVO || '',
  
    xrayClientSecret:
      process.env.XRAY_CLIENT_SECRET_TEST_NOVO || ''
  };